# node-restapi-mynotes
This is a simple rest api application which is developed in Node.js and express with Mongo DB as a database.

Pre-requisite:
`Mongo DB`
`Node.js`

# Steps to run:
  In the project directory, you can run:

### `npm install`
### `npm start`
